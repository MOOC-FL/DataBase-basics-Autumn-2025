#### Indices
- An `index` is a directory structure stored in a database table, the purpose of which is to make queries related to the table more efficient. An index allows the database system to efficiently find out where in the table there are rows that match a specific search condition.

